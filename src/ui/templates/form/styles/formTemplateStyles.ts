export const formTemplateStyles = /* css */ `
  .form-header {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: var(--measure-max);
  }
  .form-header-title {
    margin-top: var(--measure-40);
    text-align: center;
    align-self: center;
    margin-bottom: var(--measure-16);
    text-overflow: ellipsis;
    white-space: nowrap;
    width: 30ch;
    overflow: hidden;
    text-align: center;
  }
`;
