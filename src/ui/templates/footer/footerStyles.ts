export const footerStyles = /* css */ `
  footer {
    display: flex;
    justify-content: center;
    align-items: center;
    padding-top: var(--measure-24);
    padding-bottom: var(--measure-40);
    border-top-width: var(--measure-1);
    border-top-style: solid;
    border-top-color: var(--color-primary-80);
    width: var(--measure-max);
    position: fixed;
    bottom: 0;
    background-color: var(--color-primary-10);
  }
`;
