export const annotationsTemplateStyles = /* css */ `
  .annotations-title {
    font-size: var(--font-size-16);
    line-height: var(--measure-24);
    font-weight: var(--font-weight-400);
    margin-top: var(--measure-80);
    margin-bottom: var(--measure-16);
    text-overflow: ellipsis;
    white-space: nowrap;
    width: 30ch;
    height: var(--measure-24);
    overflow: hidden;
    text-align: center;
  }

`;
