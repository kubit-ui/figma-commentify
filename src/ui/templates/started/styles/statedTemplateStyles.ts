export const startedTemplateStyles = /* css */ `
  .started-header {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--measure-8);
    flex-direction: column;
    margin-top: var(--measure-80);
    margin-bottom: var(--measure-24);
  }

  .started-content {
    width: var(--measure-max);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--measure-16);
  }
`;
