export const checkboxStyles = /* css */ `
  .checkbox {
    display: flex;
    align-items: center;
    gap: var(--measure-8);
    width: var(--measure-max);
    cursor: pointer;
    padding-bottom: var(--measure-32);
  }
  .checkbox-input {
    width: fit-content;
  }
  .checkbox-label {
    font-size: var(--font-size-14);
    line-height: var(--line-height-24);
    font-weight: var(--font-weight-400);
    color: var(--color-primary-100);
    cursor: pointer;
  }
`;
